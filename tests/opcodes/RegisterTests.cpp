#include <gtest/gtest.h>

TEST(Opcodes_Registers, CLC) {
    FAIL();
}

TEST(Opcodes_Registers, CLD) {
    FAIL();
}

TEST(Opcodes_Registers, CLI) {
    FAIL();
}

TEST(Opcodes_Registers, CLV) {
    FAIL();
}

TEST(Opcodes_Registers, CMP) {
    FAIL();
}

TEST(Opcodes_Registers, CPX) {
    FAIL();
}

TEST(Opcodes_Registers, CPY) {
    FAIL();
}

TEST(Opcodes_Registers, SEC) {
    FAIL();
}

TEST(Opcodes_Registers, SED) {
    FAIL();
}

TEST(Opcodes_Registers, SEI) {
    FAIL();
}